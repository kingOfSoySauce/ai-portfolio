# GitHub 远程仓库设置

## 步骤 1：在 GitHub 创建仓库

1. 访问 https://github.com/new
2. 仓库名：`ai-portfolio`
3. 描述：`AI 产品工程师学习作品集 - RAG/Agent/LLM 应用开发`
4. 选择 Public（方便面试官查看）
5. **不要**勾选 "Add a README file"（我们已经有了）
6. 点击 "Create repository"

---

## 步骤 2：关联远程仓库并推送

```bash
# 添加远程仓库（替换 YOUR_USERNAME 为你的 GitHub 用户名）
git remote add origin https://github.com/YOUR_USERNAME/ai-portfolio.git

# 查看远程仓库
git remote -v

# 推送到 GitHub
git push -u origin master

# 查看推送结果
git log --oneline
```

---

## 步骤 3：验证

访问 `https://github.com/YOUR_USERNAME/ai-portfolio`，应该看到：
- ✅ 2 次提交记录
- ✅ README.md 显示项目说明
- ✅ 目录结构清晰
- ✅ Day1 笔记可见

---

## 提交历史

```
d747622 feat: Day1 完成 - FastAPI 工程骨架
dda7f95 chore: 初始化项目结构
```

---

## 后续每天提交规范

```bash
# Day2 完成后
git add -A
git commit -m "feat: Day2 完成 - SSE 流式输出

完成内容：
- ✅ /chat/stream 接口（SSE）
- ✅ 前端页面消费 SSE
- ✅ 逐字流式输出

验收通过：
- 浏览器看到逐字输出
- 日志记录流式请求

学习笔记：
- notes/daily/day-02.md"

git push
```

---

## 如果需要使用 SSH（推荐）

```bash
# 生成 SSH 密钥（如果还没有）
ssh-keygen -t ed25519 -C "your_email@example.com"

# 复制公钥
cat ~/.ssh/id_ed25519.pub

# 添加到 GitHub：Settings → SSH and GPG keys → New SSH key

# 修改远程仓库地址为 SSH
git remote set-url origin git@github.com:YOUR_USERNAME/ai-portfolio.git
```
